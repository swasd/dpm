---
machines:
  consul:
    driver: digitalocean
    instances: 1
    export: true
    options:
      digitalocean-image: debian-8-x64

# Encapsulation property
# This provision is privately locked to a machine provisioned by
# the provision.yml in the same package.    
