---
run:
  - docker run --net=${network}
    --name ceph-mon-0
    --ip 10.10.10.2
    -e MON_IP=10.10.10.2
    -e CEPH_PUBLIC_NETWORK=10.10.10.0/24
    -v /etc/ceph:/etc/ceph
    -v /var/lib/ceph:/var/lib/ceph
    -t ceph/daemon
    mon
