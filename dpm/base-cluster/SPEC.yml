---
specVersion: 0.1.0
spec:
  name: base-cluster
  version: 0.1.0
  title: Base cluster package
  dependencies:
    discovery: version=0.1.0  
  provision: provision.yml
  composition: composition.yml
  description: >
    This package provides a Swarm master and a set of nodes
    that form cluster via a discovery service.
    An overlay network is pre-configured 
    and available as the `multihost` network.
